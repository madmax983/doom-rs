📖 Chapter: The Core App and Renderer Utilities.
🔦 Insight: Added missing module-level documentation to `doom-app` and `doom-renderer::sprite_clip`, and silenced deprecation warnings on ratatui's `set_skip` method to make `cargo doc` warnings free.
🧪 Example: Added an executable doctest for `SpriteClipHistory::new()`.
🖼️ Preview: N/A.
