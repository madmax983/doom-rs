
📖 Chapter: Missing crate docs in `doom-app`, `doom-game::director`, and `doom-renderer::sprite_clip`.

🔦 Insight: Added narrative docs explaining AI Director health triggers, SpriteClipHistory array allocation reasoning, and empty `lib.rs` module.

🧪 Example: Added executable doctests for `AiDirector::tick` and `SpriteClipHistory::new`.

🖼️ Preview: Documentation completes missing pieces.
