🎻 Bard: [documentation update]

📖 Chapter: Added module documentation for `doom-game` components (`director.rs` and `stats.rs`).
🔦 Insight: Explained the role of the `AiDirector` in dynamic difficulty scaling and `LevelStats` for end-of-level progress tracking.
🧪 Example: Added executable doctests for `AiDirector::new` and `AiDirector::tick`.
🖼️ Preview: Documentation is now visible when running `cargo doc`.
