📖 Chapter: Documented the `sprite_clip` module in `doom-renderer`.
🔦 Insight: Explained the purpose of `SpriteClipHistory` and its methods for slicing tall sprites during masked rendering through varying portal depths.
🧪 Example: Added 4 executable doctests for `SpriteClipHistory`'s API (`new`, `push`, `last`, `iter`).
🖼️ Preview: Resolved all `-D missing_docs` warnings in `doom-renderer`.
