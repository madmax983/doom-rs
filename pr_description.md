📖 Chapter: Fix Missing Docs in `doom-renderer/src/sprite_clip.rs` and deprecated warnings in `doom-tui`.
🔦 Insight: Added missing doc comments (`///`) to `SpriteClipHistory` methods (`new`, `push`, `last`, `iter`) to satisfy `cargo doc` missing docs checks. Also suppressed deprecated warnings for `set_skip` by using `#[allow(deprecated)]`.
🧪 Example: N/A
🖼️ Preview: N/A
