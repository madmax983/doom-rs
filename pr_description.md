📖 Chapter: The `sprite_clip` and `director` modules
🔦 Insight: Explained the heap-allocation avoidance strategy for sprite clipping histories and the dynamic difficulty adjustment system of the AI Director. Silenced a deprecation warning in the TUI logic safely.
🧪 Example: Added 6 executable doctests for `SpriteClipHistory` and `AiDirector`.
