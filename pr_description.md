Title: 🎻 Bard: [documentation update]

📖 Chapter: The `director`, `sprite_clip` modules and binary crates.
🔦 Insight: Added missing module-level documentation and executable doc-tests to resolve missing_docs warnings. Also removed the empty `doom-app/src/lib.rs` to fix the false-positive missing docs warning on the binary crate.
🧪 Example: Added executable doctests for `AiDirector::new` and `SpriteClipHistory::new`.
