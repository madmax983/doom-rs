📖 Chapter: The `director` module.
🔦 Insight: Added module-level and function-level documentation to explain the AI Director, its `DirectorAction` enum, and the `AiDirector` struct.
🧪 Example: Added executable doc-tests showing how to construct and tick the AI Director.
🖼️ Preview:
