📖 Chapter: The `doom-app` and `sprite_clip` modules
🔦 Insight: Explained the architecture of the main entry point and added usage examples for the `SpriteClipHistory` algorithm to clarify its capacity limits.
🧪 Example: Added 2 executable doctests for `SpriteClipHistory` (creation and pushing items).
