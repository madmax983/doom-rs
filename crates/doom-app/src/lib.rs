//! The `doom-app` crate library root.
//!
//! This file exists to satisfy module resolution and testing constraints for the
//! `doom-app` binary. The actual entry point and master orchestrator logic is
//! located in `main.rs`.
