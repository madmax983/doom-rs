//! Doom app entry point documentation.
//!
//! Because `doom-app` uses `main.rs` as its root, we provide this dummy `lib.rs` to satisfy
//! doc tests or crate-level documentation requests without duplicate module compilation.
