//! Empty library entry point for `doom-app`.
//!
//! The `doom-app` crate is designed as the master orchestrator binary. This `lib.rs` file
//! exists because Cargo treats `src/lib.rs` as a library crate root by default when present.
