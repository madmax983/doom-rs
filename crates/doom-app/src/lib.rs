//! The root library for the doom-app crate.
//!
//! While `main.rs` serves as the entry point and orchestrator, this `lib.rs` file
//! provides the top-level crate documentation and exposes integration test points
//! if necessary. The primary narrative for the Grand Assembly happens in `main.rs`.
