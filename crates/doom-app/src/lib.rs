//! Doom application crate.
//!
//! Provides the core game loop, input handling, window management, and ties together
//! the simulation (`doom-game`), rendering (`doom-renderer`), audio (`doom-audio`),
//! and networking (`doom-net`).
