//! The `doom-app` crate acts as the "Grand Assembly" hub.
//!
//! It links rendering, audio, network, and game state. The modules within are
//! intentionally not re-exported here as they are tightly coupled with the
//! main executable logic in `main.rs`.
