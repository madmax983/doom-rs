//! Empty library root to satisfy Cargo's binary/library layout requirements.
//!
//! Because `doom-app` has both a `main.rs` and `lib.rs`, Cargo treats it as
//! a library crate as well. This documentation ensures the `-D missing_docs`
//! lint passes successfully.
