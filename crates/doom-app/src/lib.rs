//! The `doom-app` crate acts as the Grand Assembly central hub that drives decoupled subsystems
//! (rendering, audio, networking, input, game logic) and handles the event loop and demo modes.
//! It contains no core game logic or rendering algorithms itself.
