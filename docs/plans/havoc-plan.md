1. **Journal Learnings**: Add an entry to `.jules/havoc.md` documenting the deadlock and OOM vulnerabilities discovered and fixed during this chaos engineering session.
2. **Verify Changes**: Run `run_in_bash_session` to execute `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `cargo fmt --all`.
3. **Complete pre-commit steps**: Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.
4. **Submit PR**: Submit the changes with the provided PR wreckage payload description.
