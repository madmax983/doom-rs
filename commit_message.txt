⚒️ Forge: Fix Boolean Blindness in FaceState::tick

🚮 Smell: `FaceState::tick` took multiple boolean arguments (`is_firing` and `is_invulnerable`), leading to "Boolean Blindness" at the call sites where it was unclear what `true, false` or `false, true` meant.
✨ Solution: Replaced the primitive `bool` arguments with descriptive enums (`IsFiring` and `IsInvulnerable`).
🧼 Benefit: Function signatures and call sites are now strictly typed and self-documenting, significantly reducing cognitive load.
🛡️ Verification: Tests passed. No logic changed.
