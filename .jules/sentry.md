## 2026-05-24 - Stack Overflows in Map Topologies
**Learning:** `bsp.rs` suffered from stack overflows when encountering deeply nested or adversarial data. Recursion with unknown depth limits in Rust panics quickly.
**Action:** Always replace recursive tree/graph traversals with iterative equivalents using an explicit stack `Vec` and ensure cycles are prevented by tracking visited states if needed.
## 2026-05-24 - Stack Overflows in Map Topologies
**Learning:** `bsp.rs` suffered from stack overflows when encountering deeply nested or adversarial data. Recursion with unknown depth limits in Rust panics quickly.
**Action:** Always replace recursive tree/graph traversals with iterative equivalents using an explicit stack `Vec` and ensure cycles are prevented by tracking visited states if needed.
## $(date +'%Y-%m-%d') - Stack Overflows in Map Topologies
**Learning:** \`bsp.rs\` suffered from stack overflows when encountering deeply nested or adversarial data. Recursion with unknown depth limits in Rust panics quickly.
**Action:** Always replace recursive tree/graph traversals with iterative equivalents using an explicit stack \`Vec\` and ensure cycles are prevented by tracking visited states if needed.
## 2025-02-14 - Stack Overflows in Map Topologies
**Learning:** `bsp.rs` suffered from stack overflows when encountering deeply nested or adversarial data. Recursion with unknown depth limits in Rust panics quickly.
**Action:** Always replace recursive tree/graph traversals with iterative equivalents using an explicit stack `Vec` and ensure cycles are prevented by tracking visited states if needed.
