
**Use LumpName as Map keys to prevent allocations**
**Learning:** Lookups into string dictionaries using a sequence of characters usually causes overhead from String allocation or UTF-8 matching overhead. By using the new type `LumpName` (which wraps `[u8; 8]`) directly as a Map key instead of converting it to a `String`, we avoid all related overheads while preserving `Hash` and `Eq` correctness.
**Action:** Use fixed-size, byte-wrapping types like `LumpName` over `String` or `Vec<u8>` when defining lookup maps.

**[SmallVec for Hitscan Intercepts and Sector Traversal]
**Learning:** Returning `Vec<T>` or mutating `&mut Vec<T>` on hot paths like `adjacent_sectors` and `p_line_attack` causes many tiny heap allocations. `smallvec` avoids allocations for arrays up to a chosen size, dropping them on the stack. Changing `Vec::new()` to `SmallVec::new()` and setting up a sensible default capacity (e.g. 16 for `HitscanIntercept` or 8 for adjacent sectors) speeds up traversal and weapon firing significantly.
**Action:** When a function collects a small, bounded number of items (like adjacent level geometry or raycast intercepts) and is called very frequently, use `smallvec::SmallVec` instead of `Vec`.
**[Performance Optimizations - Allocations and Loom]**\n**Learning:** When trying to explicitly use  on types, ensure that the variable is actually a . In testing environments, projects may use  to test concurrency. Changing it to  will lead to  errors. Additionally, replacing dynamic  strings in hot loops with static string slice lookups via arrays is a powerful way to eliminate per-frame allocations.\n**Action:** Use  directly for structures that may alias  primitives or explicitly use . When seeing  in rendering loops, always check if the possible inputs are bounded and replace with static arrays.
**[Performance Optimizations - Allocations and Loom]**
**Learning:** When trying to explicitly use `std::sync::Arc::clone(&var)` on types, ensure that the variable is actually a `std::sync::Arc`. In testing environments, projects may use `loom::sync::Arc` to test concurrency. Changing it to `std::sync::Arc` will lead to `mismatched types` errors. Additionally, replacing dynamic `format!()` strings in hot loops with static string slice lookups via arrays is a powerful way to eliminate per-frame allocations.
**Action:** Use `.clone()` directly for structures that may alias `loom` primitives or explicitly use `loom::sync::Arc::clone`. When seeing `format!` in rendering loops, always check if the possible inputs are bounded and replace with static arrays.
